import { RotateDirectiveDirective } from './rotate-directive.directive';

describe('RotateDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
